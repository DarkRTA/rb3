#include "data.hpp"
#include "symbol.hpp"

extern DataNode* NodesAlloc(int);

DataArray::DataArray(int i){
    // size = i;
    // unka = 1;
    // unkc = 0;
    // unke = 0;
    // node = NodesAlloc(i << 3);
}